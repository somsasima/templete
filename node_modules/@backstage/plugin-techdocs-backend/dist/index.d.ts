import { Logger } from 'winston';
import express from 'express';
import Knex from 'knex';

declare type RouterOptions = {
    logger: Logger;
    database?: Knex;
};
declare function createRouter({}: RouterOptions): Promise<express.Router>;

export { createRouter };
